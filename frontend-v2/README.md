# Todo Frontend v2

Server-side rendered frontend using FastAPI + Jinja2.

## Run

```bash
cd frontend-v2
uv run uvicorn todo_frontend.main:app --host 0.0.0.0 --port 3001 --reload
```

Open http://localhost:3001
